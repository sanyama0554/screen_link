{
  "name": "@screen-link/cli",
  "version": "1.0.0",
  "description": "CLI tool for dependency analysis",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "screen-link": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc --build",
    "dev": "tsx watch src/cli.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "commander": "^11.0.0",
    "chalk": "^5.3.0",
    "ora": "^7.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "tsx": "^4.0.0",
    "eslint": "^8.0.0"
  }
}